<div class="dashboard-nav">
    <header>
        <a href="#!" class="menu-toggle"><i class="fas fa-bars"></i></a>
        <a href="/" class="brand-logo"><img src={{url_for("static", filename="img/sova_bila_mala.png")}} title="Logo Hradecká sova" alt="Logo Hradecká sova" /></a>
    </header>

    <nav class="dashboard-nav-list">
        {% if session.org %}
            <a href="#" class="dashboard-nav-item">Administrace</a>
        {% endif %}

        <a href="{{ url_for('main' ~ year ~ '.view_index') }}" title="informace o hře" class="dashboard-nav-item">Informace</a>
        <a href="{{ url_for('team' ~ year ~ '.view_teams') }}" title="Týmy" class="dashboard-nav-item">Týmy </a>

        {% if session.logged %}
            
            <div class='dashboard-nav-dropdown'><a href="#!" class="dashboard-nav-item dashboard-nav-dropdown-toggle"></i> Můj tým </a>
                <div class='dashboard-nav-dropdown-menu'>
                <a href="{{ url_for('team' ~ year ~ '.view_password_change') }}" title="Změna hesla" class="dashboard-nav-item">Změna hesla</a>
                <a href="{{ url_for('team' ~ year ~ '.view_registration_cancel') }}" title="Zrušení registrace" class="dashboard-nav-item">Zrušení registrace</a>
                <a href="{{ url_for('team' ~ year ~ '.logout_team') }}" title="Odhlášení" class="dashboard-nav-item">Odhlášení</a>
                </div>
            </div>
        {% else %}
            <a href="{{ url_for('team' ~ year ~ '.view_registration') }}" title="Registrace" class="dashboard-nav-item ">Registrace</a>
            <a href="{{ url_for('team' ~ year ~ '.login_team') }}" title="Přihlášení" class="dashboard-nav-item ">Přihlášení</a>
        {% endif%}


          <div class="nav-item-divider"></div>
          <a href="{{ url_for('main2022.view_index') }}" title="informace o hře" class="dashboard-nav-item">Ročník 2022 </a>
          <a href="{{ url_for('main2021.view_index') }}" title="informace o hře" class="dashboard-nav-item">Ročník 2021 </a>
    </nav>
</div>

